<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Aqui hacemos que gire el objeto,imagen,boton... -->
    <rotate android:fromDegrees="0"
        android:toDegrees="360"
        android:duration="2000"
        android:fillAfter="true"
        android:fromXScale="0"
        android:toXScale="2"
        android:fromYScale="0"
        android:toYScale="2"
        android:pivotX="50%"
        android:pivotY="50%"
        android:fromAlpha="0"
        android:toAlpha="1.0" />
    <!-- le damos un poco de transparencia-->
    <alpha
        android:fromAlpha="0"
        android:toAlpha="1.0"
        android:duration ="2000"/>

</set>